services:
  web:
    build: .
    restart: always
    env_file: .env
    volumes:
      - uploads_data:/app/uploads
      - db_data:/app/data
    networks:
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.upserarch.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.upserarch.entrypoints=websecure"
      - "traefik.http.routers.upserarch.tls.certresolver=cloudflare"
      - "traefik.http.services.upserarch.loadbalancer.server.port=8000"
    expose:
      - "8000"

networks:
  web:
    external: true

volumes:
  uploads_data:
  db_data:
