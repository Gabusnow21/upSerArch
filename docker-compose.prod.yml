services:
  web:
    build: .
    restart: always
    env_file: .env
    volumes:
      - uploads_data:/app/uploads
      - db_data:/app/data
    networks:
      - proxy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.upserarch.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.upserarch.entrypoints=web"
      - "traefik.http.services.upserarch.loadbalancer.server.port=8000"
    expose:
      - "8000"

networks:
  proxy-network:
    external: true

volumes:
  uploads_data:
  db_data:
